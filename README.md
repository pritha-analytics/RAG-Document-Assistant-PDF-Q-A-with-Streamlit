# RAG App

A starter Retrieval-Augmented Generation application scaffold. Replace `rag_app.py` with your actual RAG application code, add the required dependencies to `requirements.txt`, and set your OpenAI API key in `.env`.
